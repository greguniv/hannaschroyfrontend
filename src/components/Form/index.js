import { useState } from 'react';
import axios from 'axios'
import './styles.css'

const Form = () => {

    // try {
    //     const response = axios.put(`http://localhost:8080/api/v1/allcontact`)
    // } catch(error){
    //     console.log(error)
    // }

    return (
        <div>
            
        </div>
    );
}

export default Form;
